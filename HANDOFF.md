# 인계 — 지금 상태와 다음에 확인할 것

> **이 문서가 현재 상태다.** 세션이 바뀌면 여기부터 읽는다.
> 2026-08-09에 세분화를 **도서관 유지보수 엔진**으로 다시 묶고, 모델의 제안을
> 적용 전에 도메인 규칙으로 검증하도록 개편했다. **새 코드로 300건을 다시 돌리는 일이 다음 단계다.**
> 깨끗한 Python 3.12 환경 기준 **353 passed, 1 skipped · ruff · mypy 클린**이었고,
> 마지막 중복 검증 테스트 추가 뒤 관련 테스트 **59 passed**. 저장소의 기존 `.venv`는
> 사라진 Python 3.11을 가리키므로 재생성해야 한다.
> 그 뒤 192건 실측에서 축 혼합의 원인이 `Review`의 불완전한 재편임을 확인해 다시 고쳤다.
> **현재 변경 뒤 테스트는 사용자가 직접 실행하기로 했으며 아직 실행하지 않았다.**
>
> 세분화 튜닝의 **앞선 기록**(스크립트 판 8회, UI 판 3회의 수치)은
> [`docs/ralf-handoff.md`](docs/ralf-handoff.md)에 있다. 이 문서는 그 이후다.

---

## 0. 먼저 읽을 것

| | |
|---|---|
| 무엇이 참이어야 하나 | [`SPEC.md`](SPEC.md) |
| 팀 규칙·커밋 계정 | [`TEAM.md`](TEAM.md) — **커밋 전 반드시** |
| 함정 요약 | [`CLAUDE.md`](CLAUDE.md) |
| 세분화 설계 | [`docs/spec/subdivision.md`](docs/spec/subdivision.md) |

## 1. 지금 무엇을 하고 있나

**법제처 법령 300건**을 로컬 모델로 정리시키며 폴더 구조 품질을 잡는 중이다.

- 코퍼스: `C:\Users\sh\Documents\카카오톡 받은 파일\법제처_300\flat` (300건)
- **같은 폴더의 다른 하위 폴더들이 사람이 나눠본 정답지다** — 아래 §4
- 볼트: `C:\Users\sh\bismuth-vault`
- 모델: KT Cloud 프록시의 `qwen3.6-35b` (OpenAI 호환)

### 1.1 2026-08-09 유지보수 엔진 개편

- 모델은 축과 이동 계획을 **제안**하고, `domain/maintenance.py`가 전체 계획을 먼저 검증한다.
- 문장형·소진된 축, 부모와 같은 이름, 중복 이름, 알 수 없는 문서, 문서 중복 배정,
  1건짜리 부류, 전부를 한 폴더로 옮기는 무의미한 분할은 파일을 건드리기 전에 거절한다.
- 이동 경로도 전부 사전 점검한다. 계획 일부만 적용된 뒤 실패하는 상태를 만들지 않는다.
- 조상 폴더는 매번 새 부류를 묻지 않고, 증거가 두 배가 된 경계 검토만 수행한다.
- 유지보수가 실패해도 이미 완료된 문서 안착은 성공으로 남는다. 실패는 trace에 별도로 기록한다.
- 축에는 짧은 라벨뿐 아니라 사람이 읽을 질문도 저장한다. v2 노트의 문장형 오염 축은 읽을 때 폐기한다.
- `scripts/compare_vaults.py`로 입력 순서를 섞은 두 볼트의 문서 공존 관계 precision/recall/F1을 잰다.
- 아래의 기존 300건 수치와 폴더 트리는 **개편 전 기준선**이다. 새 엔진의 품질 근거가 아니다.

### 1.2 192건 재실측 뒤 추가 개편

- 192건 모두 `document.filed`, 인박스 0, 유지보수 최종 실패 1건도 인제스트는 보존됐다.
- 하지만 Review가 부모 축만 바꾸고 옛 자식을 남겨 주제·문서형식 축이 같은 레벨에 섞였다.
- 이제 부정 Review는 하위 트리의 모든 카드와 현재 경로를 읽고, 모든 문서를 정확히 한 번
  배정한 완전한 새 경계를 요구한다.
- 원본과 사이드카를 `.bismuth` 내부에 먼저 staging한 뒤 옛 하위 트리를 폐기하고 새 직속
  자식으로 재배치한다. 전체가 저널 한 건이며 실패하면 원래 경로로 복구된다.
- 기존 자식 이름은 재사용할 수 있고, 새 경계에 없는 자식은 폐기된다. 옛 하위 분류는 새 축과
  섞이지 않도록 평탄화하며 이후 증거로 다시 자란다.
- Review 사이에도 상위에 남은 문서가 기존 직속 자식 표지판에 명백히 속하면 그 폴더를
  재사용한다. 축과 이름은 바꾸지 않고, 강제 끼워맞춤 독립 검증 후 원본·사이드카를 한
  트랜잭션으로 이동한다. 애매한 문서는 상위에 그대로 둔다.
- 카드 없는 파일 또는 `managed: false` 하위 폴더가 있으면 재편을 시작하지 않는다.
- 운영 프롬프트에서 법령·부처·프로젝트 예시를 제거했다. 고정 한국어 루트 상태와 영문 단어
  블랙리스트도 제거했다.
- 의미 감사가 축이 한 속성인지, 자식명이 그 질문의 답인지, 형제가 배타적인지 적용 전에
  다시 판단한다. 순수 코드는 파일 안전성과 완전성만 판단한다.
- 완전 Review는 긴 카탈로그 해시 대신 요청 전용 `D0001` 형식 핸들을 쓴다. 먼저 현재
  경계를 직접 판정하는 boolean 3개만 받고, 하나라도 실패할 때만 완전한 교체안을 별도
  호출한다. 자유서술 `reason`은 없으며 문서 열거와 개수 검산은 코드가 수행한다.
- 경계 교체가 새 폴더에 쓴 검증된 `group.note`는 같은 인제스트의 일반 노트 갱신으로
  덮어쓰지 않는다. 기존 표지판으로 문서만 이동한 경우에만 해당 기존 폴더를 갱신한다.
- 분할된 부모의 직속 자식은 축 질문에 답하는 확정 표지판이다. 이후 노트 갱신도 검증된
  `title/purpose`를 보존한다. 배치에 쓰이지 않으면서 토큰과 오래된 설명만 늘리던
  `holds/answers`는 schema 5부터 생성·직렬화하지 않으며, 구형 노트는 계속 읽을 수 있다.
- 유지보수로 현재 문서가 다시 이동되면 사이드카에서 최종 경로를 찾아 완료 로그·진행
  표시·API 결과에 사용한다. 카탈로그의 최초 배치 경로는 현재 위치로 믿지 않는다.
- 여러 파일은 `/api/batches`가 먼저 전부 인박스에 journalled staging하고 batch ID를 반환한
  뒤, 서버 background task가 하나의 ingest lock 아래 순차 처리한다. 브라우저 연결과 작업
  수명이 분리되어 페이지를 새로고침해도 처리가 계속된다. 새 페이지는 `/api/batches`에서
  진행 중·최근 작업을 복원하고 1초마다 authoritative batch 상태를 읽는다. 단일 파일용 기존
  `/api/documents`와 수동 `/api/scan`도 같은 lock을 사용해 저널 작업이 겹치지 않는다.
- 각 문서 완료 때 `/status`·`/tree`·현재 폴더를 다시 읽고 새 중첩 폴더의 조상만 자동으로
  펼친다. progress SSE의 문서별 `done/failed`와 작업 중 3초 폴링도 보조 무효화 신호다.
  겹친 완료 신호는 하나의 await 가능한 갱신 작업으로 합친다. `/api/status`는 초기 장서가
  머무는 루트(`.`) 문서도 `placed/documents`에 포함한다.
- 여러 파일을 올리면 문서별 상세 단계 카드를 쌓지 않고 하나의 compact batch 카드에
  `완료/전체`, 현재 파일 한 줄, 전체 진행 막대, 성공·중복·인박스·실패 건수만 표시한다.
  한 파일 업로드는 진단 가치가 있으므로 기존 상세 단계를 유지한다. POST 응답보다 늦게 온
  terminal SSE가 완료 문서의 상세 카드를 다시 만들지 않도록 잠시 억제한다.
- charter schema 5는 이전 schema의 분할을 즉시 완전 Review 대상으로 표시한다. 이 감사가
  실패하면 더는 조용히 종료하지 않고 완전 경계 교체 경로로 들어간다.
- 새 직속 부류는 그 부류만 감사하지 않는다. 기존 직속 형제와 하위 트리 전체를 함께 보여
  같은 축의 답인지, 서로 겹치지 않는지, 한 단계 더 좁혀 주는지를 적용 전에 감사한다.
- 완전 교체안은 이미 실패한 경계에 대해서만 만들어지므로 별도의 `worth_relearning` 출력을
  없앴다. 현재 실패를 고치는지와 탐색성이 실제로 나아지는지 두 검사가 변경 비용을 판단한다.
- 배치 모델이 현재 폴더를 뜻해 `.` 또는 `./`를 반환하면 빈 핸들(여기 머무름)로 정규화한다.
- **192건 결과는 이 추가 개편 전 결과다. 새 코드의 품질 증거로 쓰지 않는다.**

### 1.3 2026-08-11 안정적인 폴더 표지판과 bounded 유지보수

- 기존 `_folder.md`의 `purpose`는 현재 파일 목록 요약이 아니라 분류 범위 계약으로 취급한다.
  일반 파일 추가·삭제·이동에서는 다시 생성하지 않고, 경계 생성·교체 때만 구조와 함께 쓴다.
- 220자는 프롬프트 부탁이 아니라 코드의 출력 경계다. 장문 모델 응답은 재시도하지 않으며 기존
  표지판 또는 검증된 폴더명으로 fallback한다. 노트 실패는 이미 완료된 인제스트를 실패로 바꾸지 않는다.
- 모든 자동 유지보수 프롬프트는 32,000자에서 사전 분할한다. 큰 review는 직속 표지판과 문서
  카드를 패킷별로 모두 검토하고 boolean을 fail-closed 병합한다.
- 큰 교체는 패킷별 경계 sketch → sketch reduce → 고정된 표지판에 패킷별 전체 문서 배정 →
  패킷별 의미/변경 감사 → 코드의 완전성 검증 순서다. 실제 파일 변경은 기존처럼 저널 한 건이다.
- Agent Kit도 검토했지만 이 경로는 단계와 완전성 계약이 고정되어 있어 deterministic staged
  pipeline을 사용했다. Agent Kit은 대화형 탐색과 사용자 승인형 구조 정리에 계속 사용한다.

## 2. 이번에 고친 것 — 전부 실측 근거 있음

### 세분화·배치 (트리 품질)

| 무엇 | 왜 | 커밋 |
|---|---|---|
| **배치가 폴더를 못 만든다** | 배치가 만든 폴더 20개가 **전부 문서 한 건의 제목**(`제조물 책임법`). 셋은 폴더가 2·3·6개일 때. 나머지는 모델이 "기존 트리 방식에 맞춰라"를 **정확히 지킨 결과** — 첫 실수가 다음 실수의 근거가 된다 | `c2b47dd` |
| **폴더 하나 = 축 하나** | 루트에 `과학기술`(주제)·`시행규칙`(문서 종류)·`제조물 책임법`(법령 이름)이 나란히 섰다. 어떤 이름도 나머지를 배제 못 함 | `c2b47dd` |
| **노트 재작성이 축을 지우던 것** | 노트는 문서마다, **세분화보다 먼저** 다시 그려진다. 이력을 안 옮겨서 축이 다음 도착까지밖에 못 살았다. 폴더 107개 중 기록이 남은 게 14개 | `c2b47dd` |
| **판단보다 근거를 먼저 쓰게** | **300건 물어 300번 `emerged=false`.** 같은 응답이 축(`법령의 종류`)과 부류(`법률`)를 정확히 찾아놓고서. 스키마 강제는 필드를 순서대로 생성하므로 첫 필드가 판정이면 **아무 생각 전에** 답해야 한다 | `8eda608` |
| **조상이 쓴 축은 소진** | `시행규칙/과학기술정보통신부 소관/시행규칙`, 그 옆에 `시행령`. 폴더 안에서 조상의 축은 한 값으로 고정이라 아무것도 구분 못 함 | `f1c6b99` |
| **축은 몇 단어** | Review가 `basis`에 영문 문단을 써서 축을 잡아먹었다 | `f1c6b99` |

### 모델 연결 (로컬 LLM)

| 무엇 | 왜 | 커밋 |
|---|---|---|
| 프로바이더 3개로 (Ollama 제거) | OpenAI 프로토콜을 쓰므로 호환 엔드포인트로 충분 | `f356185` |
| **추가 헤더** (`Cookie` 등) | 게이트웨이가 Bearer가 아니라 쿠키로 인증. 넣을 자리가 없었다 | `f356185`·`4f45e47` |
| 모델 목록 못 받아도 진행 | `/models`가 401이라 설정이 막혔다. 정작 아무도 안 읽는 목록 | `f356185` |
| **API 키 칸을 항상 표시** | `needs_key=False`를 "키를 넣을 수 없음"으로 구현했었다 | `ebe7419` |
| **요청 본문 추가값** | qwen thinking이 기본 ON → 문서당 93초. `chat_template_kwargs`는 비표준이라 `extra_body`로 보내야 LiteLLM의 `drop_params`를 통과 | `ea2d1c5` |
| **스키마 강제를 엔드포인트에 직접 물음** | LiteLLM 표에 없는 모델은 무조건 "미지원" → 프롬프트에 스키마 박고 수리 턴 | `41269bb` |
| 헤더도 마스킹 | 쿠키가 `bismuth.log`에 평문으로 찍혔다 | `b89643c` |

### 로그·계측

| 무엇 | 왜 | 커밋 |
|---|---|---|
| **호출별 소요시간** (`ms`) | 느린 이유를 로그 줄 간격으로 추정만 하고 있었다 | `79e2509` |
| **문서별 단계 시간** | `document.read`(파싱·카드) + `document.filed`(배치·저장·노트·세분화). 구간 표시라 합이 전체와 맞음 | `79e2509` |
| `scripts/where_the_time_went.py` | 매번 스크립트 짜지 않게 | `79e2509` |
| **테스트가 `logs/`를 덮어쓰던 것** | `LOG_DIR`이 상대경로. **조사 중이던 판의 로그를, 그 수정을 검증하려 돌린 테스트가 날렸다** | `79e2509` |

## 3. 다음에 확인할 것 — 순서대로

### 3.1 볼트 비우고 300건 다시 (제일 먼저)

`f1c6b99`의 수정(조상 축 소진, 축 길이 제한)이 **아직 실측되지 않았다.** 마지막 판은 그 전 코드다.

**볼 것:**

| 확인 | 통과 기준 | 어디서 |
|---|---|---|
| 조상과 같은 이름의 폴더 | **없어야** 함 (`시행규칙/…/시행규칙`) | `measure_vault.py` 트리 |
| 조상과 같은 축 | `subdivide.rejected` 의 `reason="axis already used above here"` | trace |
| 축이 문장인 폴더 | **없어야** 함 — 폴더 노트의 `split_basis` | 아래 명령 |
| 깊이·분기 | 사람은 깊이 2·분기 4 | `measure_vault.py` |
| 루트 잔류 | 지금 11% | 〃 |

```bash
python scripts/measure_vault.py C:/Users/sh/bismuth-vault
python scripts/where_the_time_went.py logs
```

폴더별로 기록된 축을 보려면:

```python
from bismuth.adapters.vault import FileSystemVault
from bismuth.domain.charter import CHARTER_FILENAME, Charter
v = FileSystemVault(Path(r"C:\Users\sh\bismuth-vault"))
for f in sorted(v.iter_folders(), key=lambda p: p.parts):
    if f.parts and f.parts[0] != "_inbox" and v.exists(f / CHARTER_FILENAME):
        c = Charter.from_markdown(v.read_text(f / CHARTER_FILENAME), path=f)
        print(f, "→", c.split_basis or "—")
```

### 3.2 축 순서 (미해결, 설계 판단 필요)

사람은 **소관 부처 → 법령의 종류**로 갔고 Bismuth는 **법령의 종류 → 부처**로 갔다.

종류를 먼저 쓰면 최상위 4덩어리 아래마다 **같은 부처 분할을 반복**해야 한다. 부처를 먼저 쓰면 4개 도메인이 균형 있게 갈리고 아래가 3~4개로 끝난다. 사람 쪽이 낫다.

지금 프롬프트는 *"더 많은 문서가 답할 수 있는 축을 골라라"*만 말하는데, 둘 다 300건 전부가 답할 수 있어서 구분이 안 된다. **어떤 기준으로 축의 우선순위를 물을지가 미정.**
[SPEC.md §2](SPEC.md)가 도메인 규칙 내장을 금지하므로 "법령은 부처 먼저" 같은 건 넣을 수 없다.

### 3.3 문서당 시간이 튀는 것 (원인 미확정)

문서 하나가 267초 걸린 사례가 있었는데, **문서 자체는 23초**였고 그 뒤 **루트에 던진 질문 하나가 4분**이었다. 입력이 1,233토큰뿐이라 크기 문제가 아니다.

- 공용 프록시의 서버측 지연으로 추정
- 120~130초에 몰린 덩어리가 있고 `llm_timeout_seconds`가 **120**인 게 걸린다
- **`consider_with_ancestors`가 문서마다 루트까지 올라간다** — 300건이면 루트 질문도 300번. 구조적으로 손볼 여지

`79e2509`에서 계측을 넣었으니 **다음 판이면 숫자로 나온다.** 추정하지 말 것.

### 3.4 1건짜리 폴더 (보류, 사용자 판단 대기)

한때 11번 분할 중 7번이 문서 한 건짜리 폴더를 만들었다. "2건 미만 거부"를 제안했다가 **휴리스틱이라는 지적을 받고 철회**한 이력이 있다.

그 뒤 축 관련 수정이 여럿 들어갔으니 **먼저 3.1을 돌려 얼마나 남는지 보고** 다시 판단할 것. 근거는 SPEC §3.4(*"대상이 하나면 구분할 것이 없다"*)와 프롬프트의 *"A sign in front of a single book is not a sign"* — 코드만 안 지키고 있다.

### 3.5 아직 한 번도 안 잰 것

- **순서 독립성** ([SPEC.md §3.5](SPEC.md), §6.2 첫 줄). 같은 장서를 순서만 바꿔 두 판 돌리고 **폴더 이름이 아니라 어떤 문서끼리 묶였는지**로 비교. 이 시험이 있었으면 축 문제를 훨씬 일찍 잡았다.
- **경계 다시 긋기의 안정성** — Review가 실제로 돌긴 하나 결과를 검증한 적 없다.
- **ADR** — 이번 판단들(배치 무생성, 축 하나, 근거-먼저)은 ADR-0008 개정감인데 아직 안 썼다.

### 3.6 2026-08-10 배치 상태 조회 경쟁 조건과 잘못된 영문 경계

- 배치 워커가 `_inbox` 파일을 최종 위치로 옮기는 순간 `/api/status`의
  `pending_inbox()`가 같은 파일을 읽어 `VaultError`를 낸 사례가 있었다. 파일
  처리는 계속됐고 상태 요청 하나만 500이 된 TOCTOU 경쟁 조건이다.
  `pending_inbox()`는 열거 뒤 사라진 파일만 정상 진행으로 무시하고, 여전히
  존재하는 파일의 실제 읽기 오류는 다시 발생시키도록 수정했다.
- 같은 실행에서 모델이 한국어 법령 문서들에 `Safety and Occupational Health`
  폴더와 후보 축 비교까지 담은 장문의 영문 `_folder.md`를 제안했고 기존 의미
  감사가 이를 통과시켰다. 위 상태 오류와는 직접 관계가 없다.
- 특정 말뭉치나 언어를 코드에 심지 않고, 문서 제목들의 주된 Unicode 문자
  체계와 제안 전체의 문자 체계가 명백히 다르면 경계를 거부한다. 혼합 말뭉치는
  강제하지 않는다. 폴더 설명은 320자 이하의 한 줄 탐색 표지여야 한다.
- 이 검증은 신규 경계와 전체 경계 교체 모두에 적용한다. 회귀 테스트를
  추가했지만 사용자가 직접 전체 테스트하기로 했으므로 이 세션에서는
  `compileall`과 `git diff --check`만 통과시켰다.
- 이미 만들어진 실제 볼트의 해당 폴더는 자동 수정하거나 삭제하지 않았다.
  새 코드 적용에는 서버 재시작이 필요하다.

### 3.7 2026-08-10 대규모 실행 뒤 배치·경계 교체 보강

- 238건 실행은 실패·인박스 없이 끝났지만, 배치 모델이 전체 경로 문자열을 출력하면서 `/`를
  덧붙이거나 존재하지 않는 직속 경로를 고른 거절이 반복됐다. 더 심각하게는 맞는 자식이 없는데
  유일하거나 가장 가까운 자식으로 내려가 서로 다른 문서가 강제 혼합됐다.
- 배치는 이제 루트부터 **직속 자식만 한 층씩** 고른다. 각 호출에서 정렬된 자식에 `F001` 같은
  임시 ID를 붙이고, 빈 ID면 현재 폴더에 머문다. 모델은 경로와 설명문을 생성하지 않는다.
  따라서 ID 오타·경로 발명은 파일 이동이 아니라 현재 층 정지로 끝난다.
- 기존 자식으로 상위 문서를 재배치할 때도 폴더명 대신 임시 ID를 사용한다. 코드가 ID를 실제
  이름과 검증된 노트로 되돌린 뒤에만 이동 계획을 만든다.
- 새 경계의 자식 이름이 축 이름 자체와 같으면 거절한다. `_folder.md`는 후보 축, 제외 문서,
  잔여물, 검토 과정을 설명하는 글이 아니라 그 폴더에 들어갈 문서를 긍정적으로 판별하는 짧은
  표지판이어야 한다.
- 기존 경계 전체 교체는 구조 검증·경계 감사에 더해 변경 통제 감사를 거친다. 현재 관찰된
  실패를 고치고 탐색성이 실제로 좋아지며 재학습 비용을 감수할 가치가 있다는 세 조건을 모두
  만족해야 한다. 단지 다른 분류법도 그럴듯하다는 이유로는 교체하지 않는다.
- 배치 응답의 자유 서술 `reason`은 제거했다. 배치 응답은 `folder_id`와 `confidence`뿐이며,
  API·저널 호환용 `rationale`은 코드가 만든 짧은 운영 문구라 LLM 출력 토큰을 쓰지 않는다.
- 특정 법령, 기관, 언어, 폴더 깊이·개수 임계값은 넣지 않았다. 전체 테스트는 사용자가 직접
  수행하며, 구현 세션에서는 회귀 테스트 추가 후 `compileall`과 `git diff --check`만 수행한다.

### 3.8 2026-08-10 LLM 타임아웃 뒤 6분 정지

- 설정은 120초였지만 OpenAI 호환 클라이언트의 기본 재시도 2회가 숨어 있어 실제 예외는
  약 361초 뒤에 발생했다. 이미 타임아웃된 요청이 로컬 LLM 서버에서 계속 생성되는 동안
  재전송까지 들어가 다음 요청도 대기열에 갇혔고, UI는 같은 파일의 `placing`에 고정됐다.
- 모든 LiteLLM 호출에 `max_retries=0`을 명시했다. 이제 한 번의 120초 제한 안에 실패 처리되고
  파일은 인박스에 남은 채 배치가 다음 파일로 넘어간다. 스키마가 잘못된 정상 응답을 고치는
  Bismuth의 `llm_max_schema_retries`와는 별개이며 그 동작은 유지한다.
- 전송 실패도 `llm.jsonl`에 소요시간과 함께 남기고, 타임아웃은 `ModelRequestError`로 바꿔
  터미널 전체 traceback 대신 짧은 실패 사유가 UI에 표시된다. 앱 종료 시 LiteLLM 공유 비동기
  클라이언트를 닫아 `Unclosed client session` 경고도 방지한다.

## 4. 정답지가 있다

```
C:\Users\sh\Documents\카카오톡 받은 파일\법제처_300\
  flat\                 ← 300건, 분류 전 (이걸 올린다)
  공정거래위원회\          43건 → 법률 20 · 대통령령 16 · 총리령 7
  과학기술정보통신부\       85건 → 법률 34 · 대통령령 32 · 부령 18 · 교육과학기술부령 1
  금융위원회\             86건 → 법률 36 · 대통령령 37 · 총리령 13
  중소벤처기업부\          86건 → 법률 35 · 대통령령 33 · 부령 18
```

**사람이 나눈 것: 축 두 개, 깊이 2, 최상위 분기 4.** 비교 기준으로 쓸 것.
단, [SPEC.md §2](SPEC.md)상 이 구조를 코드에 심으면 안 된다 — **모델이 스스로 도달해야** 한다.

## 5. 알아둘 실무

- **로그 파일 크기를 `Get-ChildItem`/`ls -la`로 판단하지 말 것.** Windows는 열린 핸들의 크기·수정시각을 늦게 반영한다. 0바이트로 보여서 없는 버그를 쫓은 적이 있다. `wc -c`나 읽어서 확인.
- **로컬 모델 설정**은 `~/.bismuth/config.json`. 쿠키·키가 들어있다.
- **thinking 끄기**가 필수 (문서당 93초 → 15초). 설정의 "요청 본문 추가값"에 `{"chat_template_kwargs": {"enable_thinking": false}}`.
- **비용의 88%가 카탈로깅**이다. reasoning을 만져도 총액은 24% 넘게 안 줄어든다.
- **`nano`를 reasoning에 쓰지 말 것** — 배치 단계에서 4단계 분류체계를 발명한다.
- 판당 스크립트 실행은 세션 스크래치패드의 `ralf.py` (저장소 밖). 없으면 `docs/ralf-handoff.md` §1대로 다시 만든다.

## 6. 커밋할 때

[`TEAM.md`](TEAM.md) 규칙대로 담당 영역별로 나눈다.

- `domain/`, `ports/`, `services/`, `config.py`, `container.py`, `docs/` → 최수환
- `adapters/`, `prompts/` → 김지홍
- `api/`, `cli/`, `logging_setup.py`, `tests/`, `scripts/` → 이민재

커밋 본문에는 **무엇을 실측했는지**를 적는다. AI 공동작성자 트레일러는 붙이지 않는다.
