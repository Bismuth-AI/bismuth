# Bismuth — 스펙

> **이 문서가 최상위다.** `docs/adr/`은 *왜* 그렇게 정했는지를 적는다. **무엇이 참이어야
> 하는지는 여기 있다.** 둘이 충돌하면 이 문서가 이기고, 나머지를 고친다.
>
> 코드가 이 문서와 다르면 코드가 틀린 것이다. 이 문서가 현실과 다르다고 판단되면, 코드를
> 먼저 고치지 말고 이 문서를 고치는 논의부터 한다.

---

## 1. 하는 일

**문서를 주면 사서처럼 범주로 나눠 폴더에 넣는다. 나중에 찾기 위해서다.**

찾는 주체는 사람일 수도 에이전트일 수도 있다. 어느 쪽이든 방식은 같다 — 폴더 이름과 폴더
노트를 읽고, 후보를 좁히고, 열어본다. 이 문서에서 "찾는다"는 항상 그 방식을 뜻한다.

## 2. 전제 — 누가 무엇을 찾을지 모른다

Bismuth는 이 장서를 누가 쓸지, 무엇을 물을지, 어느 업종인지 모른 채로 분류해야 한다.

이건 아쉬운 제약이 아니라 **문제의 정의**다. 도서관은 이용자를 모르고도 분류하고, 그래서
분류 체계가 도서관마다 다시 만들어지지 않는다. Bismuth도 같다.

따라서:

- **도메인 특화 규칙을 내장하지 않는다.** 업종별 분류표, 사업/연도/부서 같은 축의 하드코딩,
  특정 조직에 맞춘 범주 체계 — 전부 아니다.
- **퓨샷 예시로 분류를 가르치지 않는다.** 예시에 든 도메인 쪽으로 결과가 쏠린다.
- **휴리스틱으로 구조를 결정하지 않는다.** 판단은 모델이 현재 상태를 보고 한다(§6.1).

### 2.1 판단하는 쪽은 작은 모델이다

Bismuth가 겨냥하는 것은 **한 대에서 돌릴 수 있는 크기의 모델**이다. 이건 §2의 결과다 — 문서를
정리할 값어치가 있는 조직은 문서가 건물 밖으로 나가는지를 묻고, 그 답이 포크 없이 "아니오"여야
한다면 판단하는 모델도 그 건물 안에 있어야 한다. 현재 기준 모델은 `qwen3.6-35b`이며, 이보다
큰 모델에서만 되는 설계는 이 스펙을 만족하지 않는다.

작은 모델은 **덜 똑똑한 큰 모델이 아니라 다른 실패를 하는 모델**이다. 그래서 설계가 달라진다.

- **자율성을 주는 만큼 실패가 조용해진다.** 자유롭게 탐색하고 스스로 계획을 세우게 하면 실패가
  결과물에만 나타나고 원인이 남지 않는다.
- **하네스를 조일수록 판단이 사라진다.** 모든 것을 코드가 정하면 남는 것은 도메인 규칙이고,
  그건 §2가 금지한다.
- **그래서 경계는 "질문의 크기"에 긋는다.** 모델에게는 **한 번에 하나의 판단**만 맡기고, 그
  판단에 필요한 증거만 보여주고, 답의 **모양**은 코드가 정한다. 무엇이 참인지는 모델이 답하고,
  그 답이 파일시스템에 무엇을 해도 되는지는 코드가 판정한다.

여기서 따라 나오는 구체적 계약:

| | 왜 |
|---|---|
| **한 호출 = 한 판단** | 여러 판단을 한 응답에 담으면 그중 하나가 틀렸을 때 전부를 버려야 하고, 어느 것이 틀렸는지도 모른다 |
| **가능하면 닫힌 선택** | 문서 하나에 답 하나. 목록을 반환시키면 길이가 늘수록 누락·중복이 생긴다 |
| **스키마는 얕고 작게** | 중첩된 스키마는 제약 디코딩에서 구조를 채우느라 내용을 잃는다 |
| **필드 순서 = 생성 순서** | 근거를 먼저, 판정을 마지막에. 판정이 앞에 오면 근거를 보기 전에 결정된다 |
| **지시는 그 필드가 생성되는 시점에 실행 가능해야 한다** | 아직 없는 필드를 근거로 삼는 지시는 모델이 따를 수 없다. 표지를 먼저 생성하면서 "표지가 이름과 같으면 안 된다"고 쓰면, 그 시점엔 피할 이름이 없다 — 300건 라운드에서 표지 50개가 이름과 같게 돌아왔다. 판정 필드의 지시도 마찬가지로 **이미 쓴 필드를 읽으라고** 해야 한다 |
| **스키마 강제 시 필드 설명은 모델에 닿지 않을 수 있다** | 계약은 프롬프트 본문에도 적는다. 구조는 스키마가, 의미는 프롬프트가 전달한다 |
| **필드 길이 상한은 지시로 읽힌다** | 의미 필드에 문자 수 상한을 두지 않는다. 생성 한도는 전송 계층의 회로 차단기이지 의미 계약이 아니다 |
| **분류 호출의 샘플링은 애플리케이션이 정한다** | 채팅용으로 맞춘 값(높은 temperature, presence penalty)은 분류를 망가뜨린다. 특히 presence penalty는 "기존 폴더를 재사용하라"는 요구와 정면으로 충돌한다 |

**이 표의 항목은 전부 관측에서 나왔다.** 근거는 [ADR-0016](docs/adr/0016-signs-that-discriminate-and-names-that-fit.md)과
[ADR-0017](docs/adr/0017-small-model-contract.md)에 있다.

## 3. 여기서 따라 나오는 것들

아래는 취향이 아니라 §1·§2에서 유도된다. 유도가 안 되는 항목은 여기 있으면 안 된다.

### 3.1 분류 근거는 문서 자체

이 장서를 누가 쓰는지가 아니라, 이 문서가 **무엇에 관한 것인지**로 분류한다. 파일명·경로·
확장자는 근거가 아니라 힌트다 — `final_v3_REAL.pdf`는 제목이 아니다.

### 3.2 범주는 장서에서 자란다

미리 만든 범주 목록이 없다. 있는 것으로부터 범주가 생긴다. 장서에 계약서만 있으면 계약서를
나누는 축이 생기고, 논문만 있으면 논문을 나누는 축이 생긴다.

#### 장서 전체를 삼키는 속성은 첫 축이 아니다

**문서 형식, 언어, 날짜, 발행 기관** 같은 속성은 거의 모든 문서가 값을 가진다. 그래서 트리를
빈틈없이 채우고, 빠르게 답이 나오고, **찾는 쪽에게는 거의 쓸모가 없다.** 이용자는 "이 문서가
시행규칙인가"를 알고 싶어서 오는 게 아니라 무엇에 관한 문서인지를 찾으러 온다.

이건 그 속성들이 틀렸다는 뜻이 아니라 **순서의 문제**다. 이런 축은 이미 무엇에 관한 것인지로
좁혀진 다음, 그 안에서 더 나눌 것이 남았을 때 쓸 수 있다. 먼저 쓰면 주제가 그 아래로 흩어져
어느 가지를 내려가도 다시 전수 검색이 된다.

- **첫 경계는 문서가 무엇에 관한 것인지를 묻는다** (§3.1).
- 형식·언어·날짜·기관은 그것만으로 첫 축이 될 수 없다.
- 판정은 모델이 한다. 코드가 축 이름을 어휘로 검사하면 그게 §2가 금지한 도메인 규칙이 된다 —
  이 조항은 프롬프트와 의미 감사에 실린다.

### 3.3 훑을 수 있어야 한다

찾는 방식이 "읽고, 좁히고, 열어본다"이므로 **두 가지가 동시에 참**이어야 한다:

| | 깨지면 | 한계의 출처 |
|---|---|---|
| **어떤 폴더도 너무 크지 않다** | 그 안에서 다시 전수 검색 — 정리한 의미가 없어짐 | 한 폴더의 목록이 한 번에 다뤄질 수 있는 크기 |
| **어떤 층도 너무 넓지 않다** | 형제 폴더 카드를 다 못 읽어서 고를 수 없음 | 형제 카드 전체가 한 번에 읽힐 수 있는 양 |

**두 제약은 서로 반대로 당긴다.** 폴더를 잘게 쪼개면 층이 넓어지고, 층을 좁히면 폴더가
커진다. 둘 다 만족시키는 방법은 **깊이**뿐이다. 그래서 트리는 넓지도 크지도 않고 깊다.

한계값은 사람의 감이 아니라 **읽는 쪽의 예산**에서 나온다. 이 값들은 §6의 검증 기준이지
코드의 판단 기준이 아니다(§6.1).

#### 3.3.1 읽는 쪽은 파일시스템 도구를 든 에이전트다

§1이 말하는 "나중에 찾기"의 주체는 사람이 아니라 **`ls`·`grep`·`read`만 가진 에이전트**다.
예산은 그 셋에서 나온다. 에이전트의 반복은 이렇다.

```text
ls <폴더>     하위 폴더 이름 + 각 노트 + 여기 남은 문서
              → 어디로 내려갈까 / 여기서 grep할까 / 여기서 읽을까
내려가서 다시 ls
...
잎에 도착     ls → 후보를 grep하거나 사이드카를 read
```

| 비용 | 왜 비싼가 |
|---|---|
| **`ls` 한 번** | 도구 호출 한 번이고, 그 목록 전체가 작업 기억에 올라간다 |
| **한 번 내려가기** | 잘못 내려간 것을 알아채기 어렵고, 알아채도 왕복이 다시 든다 |
| **`grep`의 범위** | 트리가 존재하는 이유다. 하위 트리로 좁히지 못하면 grep은 장서 전체를 훑는다 |

여기서 셋이 따라 나온다.

**한 층의 폭은 한 번의 판단이 감당할 양이다.** 형제가 서넛 미만이면 첫 선택이 걸러내는 게 적어
깊이만 늘고, 스물을 넘으면 에이전트는 이름 목록을 다시 훑는 것이며 그건 폴더가 없애려던 바로
그 일이다.

**깊이는 곱해진다.** 한 층의 선택이 90% 맞아도 3층이면 73%, 5층이면 59%다. 넓힐 수 없으니
깊게 가야 하지만 깊이도 공짜가 아니다.

**잎은 grep이 쓸 만한 크기여야 한다.** 수십 건이면 grep 결과가 다시 목록이 되고, 한두 건이면
판단 한 번을 쓰고 아무것도 아끼지 못한 것이다.

##### 권장 형태

이 셋은 곱해져 장서 크기를 정한다. 그래서 하나의 정답이 아니라 **크기에 따라 움직이는 띠**다.
아래는 개선을 재는 기준선이다 — 벤치마크의 정답 데이터셋에 해당한다.

| 장서 크기 | 층당 폭 | 깊이 | 잎 문서 수 |
|---|---|---|---|
| ~100 | 3–5 | 1–2 | 8–20 |
| ~1,000 | 5–9 | 2–3 | 10–25 |
| ~10,000 | 6–10 | 3–4 | 15–30 |
| ~100,000 | 8–12 | 4–5 | 15–30 |

절대 상한: **폭 20, 깊이 5, 잎 50.** 하나라도 넘으면 그 가지는 실패다.

##### 크기와 무관하게 항상 참이어야 하는 것

띠는 장서 크기를 타지만 아래는 안 탄다. 개수가 아니라 형태의 문제다.

- **통과 폴더가 없다.** 모든 폴더는 문서를 담고 있거나, 자식이 둘 이상이거나, 둘 중 하나다.
  문서를 거의 갖지 않으면서 자식 하나만 둔 폴더는 `ls`를 한 번 더 시키고 아무것도 좁히지 않는다.
- **한 폴더에 남은 더미가 가장 큰 자식보다 크지 않다.** 크면 꺼내진 부류가 소수이고 대부분은
  여전히 더미다 — 나눔이 일어나지 않았다.
- **형제는 서로 배타적이다.** 둘 다 후보가 되는 이름이 나란히 있으면 에이전트는 둘 다 연다.
  개수를 아무리 맞춰도 이게 깨지면 실질 분기 수가 두 배가 된다.
- **노트만으로 배제할 수 있다.** 안에 뭐가 있는지 알려고 폴더를 열어봐야 한다면 그 노트가 제
  일을 못 한 것이다. 노트의 일은 설명이 아니라 배제다(§3.6).
- **경로가 도구 출력에서 읽힌다.** 모든 `grep` 결과 줄과 `ls` 출력에 경로가 실린다. 한 마디가
  길면 네 층만 내려가도 줄이 접힌다.

### 3.4 넓게 넣고, 자라면 쪼갠다

**이것이 Bismuth의 핵심 동작이며, 배치 정확도보다 중요하다.**

네 동작이 다 있어야 한다. 앞의 둘은 문서를 옮기고, 뒤의 둘은 폴더를 옮긴다.

- **배치** — 새 문서를 현재 트리의 맞는 자리에 넣는다.
- **세분화** — 폴더 안에 구분이 보이면 하위로 나누고, 그 안의 문서들을 옮긴다.
- **묶어 올리기** — 너무 넓어진 한 층의 폴더 몇을 하나의 상위 선반 아래로 모은다.
- **층 없애기** — 값을 못 하는 층을 지우고, 그 안의 폴더와 문서를 한 단 위로 올린다.

**뒤의 둘은 서로의 역연산이고, 그래서 앞선 판단의 실수를 되돌린다.** 이건 우리가 발명한 게
아니라 Cobweb(1987)이 증분 개념 형성의 네 연산으로 세운 것이다
([`docs/prior-art.md`](docs/prior-art.md), [ADR-0018](docs/adr/0018-maintenance-is-four-operators.md)).

세분화가 없으면 첫 배치가 영구적이 되고, 그래서 **먼저 들어온 문서가 트리를 결정한다.**
층 없애기가 없으면 첫 **세분화**가 영구적이 되고, 그래서 **먼저 그은 층이 트리를 결정한다.**
둘 다 있어야 첫 판단이 잠정값이 된다.

**그리고 이 네 연산자가 프롬프트를 짧게 만든다.** 잘못 그은 경계의 대가가 무한하면 프롬프트는
그것을 막으려고 길어질 수밖에 없다. 되돌릴 수 있으면 나쁜 이름의 대가는 나중 연산 한 번이고,
**일찍 틀릴 권리를 사는 것**이 이 설계가 사는 것이다.

#### 문서 하나로는 분류할 수 없다

우리가 만드는 것은 분류표가 이미 있는 도서관이 아니라 **이제 막 생기는 도서관**이다. 책은
한 권씩 들어오고, 분류 체계는 아직 없다(§3.2).

**분류는 구분이고, 대상이 하나면 구분할 것이 없다.** 문서 한 건을 보고 지은 폴더 이름은
그 문서 말고는 아무것도 담지 못하며, 그 뒤에 오는 모든 문서가 그 이름과 비교당한다.

그러므로:

- **첫 문서로 폴더를 만들지 않는다.** 나눌 만한 구분이 보일 때까지 루트에 둔다.
- **루트도 쪼개지는 폴더다.** 특별하지 않다. 루트가 커지면 루트를 쪼갠다.
- **문서 열 건짜리 장서에 폴더가 없는 것은 미완성이 아니라 그 규모의 정답이다.** 열 건은
  그냥 다 보인다.

이건 "충분히 쌓일 때까지 기다린다"가 아니다. 문서는 즉시 처리되고(카드·사이드카·검색 가능)
즉시 놓인다. 놓이는 자리가 아직 루트일 뿐이다.

#### 확신이 없으면 루트다

**"어디 둘지 모르겠다"의 올바른 답은 대기가 아니라 루트다.** 루트는 아직 구분이 그어지지
않은 자리이고, 확신이 없다는 건 정확히 그 뜻이다. 그러니 배치에 **확신 임계값이 없다** —
넘고 못 넘고를 가릴 선이 없다. 확신도는 기록하되 문을 막는 데 쓰지 않는다.

**인박스에 남는 것은 읽지 못한 문서뿐이다.** 스캔본, 깨진 파일, 지원하지 않는 형식. 읽을 수
있으면 무엇이든 놓인다. 읽지 못한 것을 루트에 섞으면 세분화가 잡음에서 범주를 만들려 든다.

#### 배치는 트리를 한 층씩 걷는다

배치는 전체 경로를 새로 쓰지 않는다. 현재 폴더의 **직속 자식 표지판만** 읽고 하나를 고르거나
현재 폴더에 머문다. 자식을 골랐을 때만 다음 층에서 같은 판단을 반복한다. 따라서 어떤 부모를
골랐다고 해서 그 아래의 유일한 자식까지 강제로 내려가지 않는다.

모델에게는 폴더명 대신 그 요청에서만 유효한 짧은 ID를 준다. 애플리케이션이 ID를 실제 폴더로
해석하므로 슬래시 추가, 번역, 철자 오류, 존재하지 않는 경로 발명이 파일 작업으로 이어지지
않는다. 모르는 ID는 유효한 선택으로 보정하지 않고 아래의 실패 경로로 보낸다.

#### 세분화는 이름도 고치고 문서도 옮긴다

- **이름은 표본이 아니라 부류를 가리킨다.** 폴더 이름은 그 폴더가 담게 될 것들의 공통점이지
  그 안 문서 하나의 제목이 아니다.
- **쪼갤 때 부모 이름도 다시 본다.** 내용이 이름과 어긋나 있으면 이름을 고친다. 쪼개기만
  해서는 잘못 붙은 이름이 남는다.
- **세분화는 그 폴더에 이미 있던 문서를 재배치한다.** 새로 들어오는 문서에만 적용하면 먼저
  들어온 문서가 자리를 차지한 상태가 그대로 남고, §3.5가 성립하지 않는다.
- **기존 경계를 교체하는 기준은 '다른 분류도 가능함'이 아니라 '현재 실패를 실제로 고침'이다.**
  새 계획은 현재 표지판과 나란히 비교해 탐색성이 명백히 좋아지고, 이용자가 구조를 다시 익힐
  비용을 감수할 가치가 있을 때만 적용한다.
- **폴더 이름은 축의 답이어야 한다.** `규정 대상` 같은 축 이름 자체를 자식 폴더명으로 반복한
  계획은 구분을 만들지 못하므로 적용하지 않는다. 폴더 노트도 후보·잔여·검토 과정을 늘어놓지
  않고 그 폴더에 들어갈 문서를 긍정적으로 판별하는 짧은 표지판이어야 한다.
- **폴더 노트는 현재 목록의 요약이 아니라 안정적인 범위 계약이다.** 파일이 하나 추가·삭제·이동될
  때마다 다시 쓰지 않는다. 누적된 증거가 기존 범위를 반박해 경계를 새로 만들거나 교체할 때만
  구조와 함께 바꾼다. 모델의 길이 지시는 신뢰하지 않고 애플리케이션이 표지판 상한과 실패 격리를
  보장한다.
- **장서 전체성은 단일 컨텍스트를 뜻하지 않는다.** 큰 경계는 모든 직속 표지판과 모든 문서 카드를
  제한된 패킷에서 한 번 이상 검토하고, 축약·병합과 패킷별 배정을 거친다. 코드는 마지막에 모든
  문서가 정확히 한 번 배정됐는지 검증한 뒤에만 하나의 트랜잭션으로 적용한다.

### 3.4.1 모델 출력도 신뢰 경계 안에 둔다

배치 판단은 JSON 객체가 아니라 요청마다 제시된 자식 ID, `STAY`, `UNREADABLE` 중 하나만
받는다. 앞뒤 공백과 ASCII 대소문자 외에는 보정하지 않는다. 설명, JSON, 따옴표, 존재하지
않는 ID는 유효한 배치가 아니다. 한 번의 깨끗한 재요청에도 유효한 값이 없으면 원본은
`_inbox`에 남으며, 검증된 선택만 파일을 이동시킬 수 있다. 모델이 보고하는 confidence는
배치 근거도 안전장치도 아니므로 요청하거나 저장하지 않는다.

모든 모델 생성에는 작업별 출력 토큰 상한이 있다. 구조화 호출은 전체 응답을 로그에
보존하되 복구 요청에 넣는 실패 출력은 제한한다. 스트림은 마지막 청크 이후의 inactivity
제한과, 출력이 계속되어도 갱신되지 않는 절대 실행시간 제한을 함께 적용한다. 명백한 반복
출력은 클라이언트가 중단하며 전송 계층의 자동 재시도는 하지 않는다.

### 3.5 순서에 진 것을 되돌릴 수 있어야 한다

**같은 장서를 다른 순서로 넣으면 다른 트리가 나온다. 그것을 고칠 수 있어야 한다.**

이 절은 전에 "다른 순서로 넣으면 비슷한 트리가 나와야 한다"였다. 그 요구는 만족될 수 없다.
증분 클러스터링에서 제시 순서가 결과를 바꾸는 것은 알려진 성질이고, 어떤 순서로도 도달할 수
없는 구조가 존재한다는 결과까지 있다([`docs/prior-art.md`](docs/prior-art.md)). 측정으로도
그렇다 — `temperature=0`에서 같은 코드·같은 문서·같은 순서로 두 번 돌려도 다른 트리가 나온다.

그러므로 요구는 **불변**이 아니라 **회복 가능성**으로 쓴다.

- 어떤 층도 영구적이지 않다. 그은 층은 지울 수 있다(§3.4의 네 연산자).
- 어떤 폴더도 영영 닫히지 않는다. 문서가 더 안 들어와도 다시 물어볼 길이 있어야 한다.
- 전체 트리를 다시 보는 패스가 있다. 이건 증분 경로가 실패했다는 뜻이 아니라, 이 부류의
  알고리즘이 순서 의존성에 쓰는 표준 후처리다.

**주기적 개정은 결함이 아니라 단일 계층의 성질이다.** 랑가나단이 패싯을 만든 이유가 그것이고,
파일시스템이 제품인 이상(ADR-0001) 우리는 단일 계층이다. 목표는 개정이 필요 없는 구조가
아니라 **개정이 감당 가능한 구조**다 — 그래서 §6.2가 재설계의 비용을 잰다.

### 3.6 폴더 노트는 판별식이다

폴더 노트는 장식이 아니라 **찾는 쪽이 후보를 좁히는 데 쓰는 유일한 단서**다. 그래서 "여기
무엇이 있다"만으로는 부족하고, **형제 폴더가 아니라 왜 여기인지**가 서 있어야 한다. 배제할
수 있어야 훑는 비용이 준다.

### 3.7 결과물은 진짜 파일이다

폴더와 파일이 산출물이다. Bismuth를 지워도 정리된 문서는 남는다. 인덱스나 DB에만 존재하는
구조는 이 스펙을 만족하지 않는다 — 찾는 쪽이 `ls`/`read`/`grep`만 있어도 되어야 한다.

## 4. 불변식

깨지면 버그다.

1. **원본은 수정되지 않는다.** 이동은 하되 내용은 건드리지 않는다.
2. **모든 변경은 되돌릴 수 있다.**
3. **문서를 잃지 않는다.** 어떤 실패에서도 파일은 남는다.
4. **읽지 못한 문서는 조용히 사라지지 않는다.** 인박스에서 눈에 띄게 기다린다.
5. **구조는 디스크가 진실이다.** 캐시와 디스크가 갈리면 디스크를 따른다.

## 5. 비목표

명시적으로 하지 않는 것. 이유가 붙어 있고, 이유가 사라지면 다시 논의한다.

- **특정 도메인·조직·사업에 맞춘 분류** — §2.
- **사람이 구조를 미리 정하는 단계** — 현재 범위 밖. 사서 역할은 AI가 한다. 사람의 교정·
  승인은 고도화 영역으로 미룬다.
- **완벽한 첫 배치** — §3.4가 첫 배치를 잠정값으로 만든다. 첫 판단을 더 똑똑하게 만드는
  쪽으로 문제를 풀지 않는다.
- **여러 트리 동시 유지** — 파일시스템은 트리 하나다. 다른 축으로 찾는 것은 노트와
  사이드카가 grep 되는 것으로 해결한다. (이 근거는 §7에서 다시 열려 있다 — 단일 계층이
  주기적 개정을 요구한다면 비목표의 값이 달라진다.)
- **검색 자체** — Bismuth는 찾기 좋은 구조를 만든다. 찾는 것은 그 구조를 쓰는 쪽의 일이다.

## 6. 수용 기준

### 6.1 임계값은 검증 기준이지 판단 기준이 아니다

**코드는 "50개가 넘으면 쪼갠다" 같은 규칙으로 판단하지 않는다.** 구조를 나눌지 말지는 모델이
현재 상태를 보고 판단한다. 개수를 세어 임계값과 비교하는 것은 도메인마다 맞는 값이 달라
휴리스틱이 되고, §2를 어긴다.

아래 숫자는 **결과가 쓸 만한지 밖에서 재는 자**다. 코드가 이 숫자를 읽어서는 안 된다.

### 6.2 측정 항목

§3.3.1의 권장 형태가 이 표의 기준선이다. 형태 항목은 거기서 그대로 내려온다.

| 항목 | 재는 법 | 목표 |
|---|---|---|
| **잎 크기** | 잎 폴더별 문서 수의 중앙값·최대값 | 장서 크기에 맞는 띠 안, 최대값이 50 아래 |
| **층별 분기 수** | 각 폴더의 직속 하위 폴더 수의 최대값 | 띠 안, 20 아래 |
| **깊이** | 가장 깊은 잎까지의 층 수 | 띠 안, 5 아래 |
| **통과 폴더** | 문서 2건 미만이면서 자식이 하나뿐인 폴더 수 | **0** |
| **미분해 더미** | 남은 더미가 가장 큰 자식보다 큰 폴더 수 | **0** |
| **미분류율** | 인박스에 남은 문서 / 전체 | 읽지 못한 문서만. 그 외 0 |
| **순서 독립성** | 같은 장서를 서로 다른 순서로 두 번 인제스트. **문서 쌍이 같은 폴더에 묶였는지로 비교한다** — 폴더 이름이 아니라 묶임이 같은지를 본다 | 미정 — 두 합당한 분류는 원리상 낮게 나온다. 절대값보다 **추세**로 본다 |
| **검색 정확도** | 질의 집합에 대해, 찾는 쪽이 정답 폴더를 1순위로 고르는 비율 | 미정 — 질의 집합부터 필요 |
| **닫힌 폴더** | 다시 물어볼 일정이 하나도 안 걸린 폴더 수 / 전체 | **0** — 얼어붙은 폴더는 §3.5의 회복 가능성을 깬다 |
| **버려진 판단** | 모델에게 묻고 답을 버린 횟수 / 전체 결정 수. `scripts/guards.py` | 미정 — 한 실행에서 91~99%였다. 안전망이 대부분의 일을 하고 있다는 뜻이고, 그 자체가 설계 신호다 |
| **재설계 비용** | 전면 재설계 한 번이 쓰는 입력 토큰 / 처음 정리에 쓴 입력 토큰 | 미정 — 문서 수가 아니라 **폴더 수**를 따라야 한다(ADR-0018) |
| **진동** | 같은 폴더가 만들어졌다 없어졌다 하는 횟수 | **0** — 묶어 올리기와 층 없애기는 역연산이라 서로를 되돌릴 수 있다 |

형태 항목은 모델 호출 없이 잰다. 트리만 보면 되므로 판마다 즉시 비교할 수 있고, 그래서 개선의
기준선이 된다.

**마지막 네 항목은 결과물이 아니라 유지보수 자체를 잰다.** 트리만 보면 아무것도 안 지어진
실행과 잘 지어진 실행이 똑같아 보인다 — 거부는 안전망이 하라고 있는 일이라 오류로 찍히지
않는다. 한 실행이 판단의 96%를 버리면서 폴더 하나를 지었고, 그 실행은 오류 0으로 끝났다.

**목표값이 "미정"인 항목은 아직 정하지 않았다는 뜻이고, 그 자체가 스펙의 일부다.** 지어낸
숫자를 채우지 않는다. 현재 값을 먼저 측정하고, 개선 폭으로 정한다.

### 6.3 디버깅과 개선은 실제 실행 증거에서 시작한다

> **모든 원인 분석은 그 실행의 실제 로그를 열어서 한다. 예외 없다.**
>
> 모델에게 실제로 들어간 입력과 실제로 나온 출력을 보지 않고 원인을 말했다면 그것은 가설이고,
> 가설만으로 의미 로직을 바꾸지 않는다. 결과 트리·개수·이벤트 집계·소스 코드는 **어디가**
> 잘못됐는지는 알려주지만 **왜**는 증명하지 않는다.
>
> 실측된 대가: 300건 튜닝 라운드를 열여덟 번 돌리는 동안 호출 기록 25,720건(287MB)을 한 번도
> 열지 않았다. 마침내 하나를 열었을 때 그 안에 결함이 넷 있었고, 그중 셋은 집계로는 보이지
> 않았으며 하나는 이벤트조차 없었다. 열여덟 라운드가 못 찾은 것이 호출 하나에 있었다.
>
> 로그 구조는 [ADR-0015](docs/adr/0015-run-scoped-diagnostics.md), 절차는 아래 6.3.

LLM 동작, 분류 품질, 실패, 지연을 분석할 때 최종 UI 문구, 결과 트리, 토큰 합계, 소스 코드만으로
원인을 단정하지 않는다. 그것들은 결과와 가설을 알려 주지만, 모델이 실제로 무엇을 보고 어떤 판단을
했는지는 증명하지 않는다.

원인 분석과 그에 따른 개선 작업 전에 최소한 다음 순서로 해당 실행을 재구성한다.

1. 분석할 실행의 시각, run ID, stage, window와 문서 ID를 먼저 특정한다.
2. 각 단계에서 모델에게 **실제로 전달된 입력**을 확인한다. system/user 메시지, 스키마, 모델과 생성
   설정, 그 요청 안에서만 유효했던 핸들을 포함한다.
3. 그 입력에 대한 **실제 출력**을 확인한다. raw content와 reasoning content, finish reason,
   잘림·반복·timeout·transport 오류를 포함한다.
4. 모델 출력 뒤의 애플리케이션 처리를 연결한다. 결정론적 검증, 의미 감사, 적용 또는 무변경 결정,
   저널 엔트리와 실제 파일시스템 결과를 순서대로 대조한다.
5. 지연 분석은 단계별 시작·종료 시각과 호출별 elapsed time으로 계산한다. 로그 줄 간격이나 총 토큰만
   보고 어느 단계가 느렸다고 추정하지 않는다.

분석 보고서는 단계별로 `실제 입력 → 실제 출력 → 검증 결과 → 최종 영향`을 추적할 수 있어야 한다.
관측된 사실, 코드에서 도출한 해석, 아직 증거가 없는 가설을 명시적으로 구분한다. raw 로그가 없거나
실행 ID를 연결할 수 없으면 그 범위의 분석은 **미확인**이라고 보고하고, 추측을 확정 원인처럼 말하거나
그 추측만으로 의미 로직을 변경하지 않는다.

**따라서 로그는 재시작으로 사라지면 안 되고, 한 줄이 그 자체로 조인 가능해야 한다.** 실행마다
고유한 run ID, 호출마다 열 수 있는 정확한 입력·출력, 단계와 문서를 잇는 ID가 이 조항의 최소 요건이다
([ADR-0015](docs/adr/0015-run-scoped-diagnostics.md)).

로그 파일이 비어 있는지도 디렉터리 메타데이터만으로 판단하지 않는다. 실제 byte stream을 읽거나
신뢰할 수 있는 byte count로 확인한다. 특히 실행 중인 Windows 파일의 표시 크기와 수정 시각은 늦게
갱신될 수 있다.

개선안은 위에서 관측한 실패 단계와 변경할 코드 경계를 일대일로 연결해야 한다. 가능한 경우 같은
입력에서 축약한 재현 fixture로 변경 전 실패와 변경 후 결과를 비교한다. Fake LLM 테스트와 정적 검사는
계약과 안전성을 검증할 수 있지만 **실제 모델의 분류 품질이 개선됐다는 근거로 대체하지 않는다.**

반복 출력은 기본적으로 sampling 값이나 사후 문자열 제거로 해결하지 않는다. 먼저 반복이 시작된 시점의
실행 상태를 재구성해, 그 단계에서 자유서술이 허용되었는지, 배열만 제한되고 항목 문자열은 무제한이었는지를
확인한다. 단계의 스키마 계약을 좁혀 같은 생성 상태가 생기지 않게 하는 것이 우선이며, 반복 감지와 출력
한도는 계약 위반을 기록하고 비용을 제한하는 회로 차단기다(§3.4.1).

### 6.4 이 스펙이 통과했다고 말할 수 있는 순간

장서를 통째로 넣었을 때, 그 결과 트리 위에서 찾는 쪽이 **정리 전보다 확실히 잘 찾고**,
**순서를 바꿔 다시 넣어도 그 성질이 유지되는** 때다. 나머지 항목은 그 두 가지를 무너뜨리지
않았는지 보는 보조 지표다.

## 7. 아직 정하지 않은 것

여기 있는 항목은 "모른다"가 아니라 **"의도적으로 미룸"**이다.

세분화의 설계는 [`docs/spec/subdivision.md`](docs/spec/subdivision.md)에 있다. 거기서도
안 정한 것은 그 문서 끝에 적혀 있다.

- **인박스가 어디까지 남나** — 루트가 정상 답이 되면(§3.4) "확신이 낮아 파킹"은 대부분
  사라진다. 읽을 수 없는 문서만 남을 텐데 그 선을 아직 안 그었다.
- **§6.2의 목표값들** — 측정 후.
- **사람의 교정 지점** — §5에서 미룬 것.
- **폴더를 다시 물어볼 두 번째 일정** — 지금은 "문서가 두 배로 늘면"뿐이다. 그래서 한 번도
  나뉜 적 없는 폴더는 대상조차 아니고(한 실행에서 55개 중 42개), 문서가 더 안 들어오는
  폴더는 영영 안 열린다. 값싼 신호로 무엇이 변했는지 재는 방향을 ADR-0018에 적었으나 아직
  구현하지 않았다.
- **패싯** — §5의 "여러 트리 동시 유지"는 파일시스템이 트리 하나라는 이유로 비목표였다.
  랑가나단의 진단은 단일 계층이 주기적 완전 개정을 요구한다는 것이고, 그게 사실이라면
  비목표의 근거가 "그러면 개정을 받아들인다"로 바뀐다. 사이드카가 이미 grep 되므로 두 번째
  축을 색인으로 갖는 것은 기술적으로 가능하다. 결정하지 않았다.

## 8. 용어

| 말 | 뜻 |
|---|---|
| **장서** | 정리 대상 문서 전체 |
| **카드** | 문서 하나에 대해 모델이 파악한 것 (무엇인지, 무엇에 관한 것인지) |
| **폴더 노트** | 폴더 하나가 무엇을 담는지에 대한 서술. 찾는 쪽의 판별식(§3.6) |
| **사이드카** | 원본 옆에 두는 grep 가능한 텍스트 |
| **배치** | 새 문서를 트리의 자리에 넣는 것 |
| **세분화** | 커진 폴더를 하위로 나누는 것 |
| **찾는 쪽** | 이 구조 위에서 검색하는 사람 또는 에이전트 |
