# Bismuth — 스펙

> **이 문서가 최상위다.** `CLAUDE.md`는 이 저장소에서 *어떻게* 작업하는지를, `docs/adr/`은
> *왜* 그렇게 정했는지를 적는다. **무엇이 참이어야 하는지는 여기 있다.** 셋이 충돌하면 이
> 문서가 이기고, 나머지를 고친다.
>
> 코드가 이 문서와 다르면 코드가 틀린 것이다. 이 문서가 현실과 다르다고 판단되면, 코드를
> 먼저 고치지 말고 이 문서를 고치는 논의부터 한다.

---

## 1. 하는 일

**문서를 주면 사서처럼 범주로 나눠 폴더에 넣는다. 나중에 찾기 위해서다.**

찾는 주체는 사람일 수도 에이전트일 수도 있다. 어느 쪽이든 방식은 같다 — 폴더 이름과 폴더
노트를 읽고, 후보를 좁히고, 열어본다. 이 문서에서 "찾는다"는 항상 그 방식을 뜻한다.

## 2. 전제 — 누가 무엇을 찾을지 모른다

Bismuth는 이 장서를 누가 쓸지, 무엇을 물을지, 어느 업종인지 모른 채로 분류해야 한다.

이건 아쉬운 제약이 아니라 **문제의 정의**다. 도서관은 이용자를 모르고도 분류하고, 그래서
분류 체계가 도서관마다 다시 만들어지지 않는다. Bismuth도 같다.

따라서:

- **도메인 특화 규칙을 내장하지 않는다.** 업종별 분류표, 사업/연도/부서 같은 축의 하드코딩,
  특정 조직에 맞춘 범주 체계 — 전부 아니다.
- **퓨샷 예시로 분류를 가르치지 않는다.** 예시에 든 도메인 쪽으로 결과가 쏠린다.
- **휴리스틱으로 구조를 결정하지 않는다.** 판단은 모델이 현재 상태를 보고 한다(§6.1).

## 3. 여기서 따라 나오는 것들

아래는 취향이 아니라 §1·§2에서 유도된다. 유도가 안 되는 항목은 여기 있으면 안 된다.

### 3.1 분류 근거는 문서 자체

이 장서를 누가 쓰는지가 아니라, 이 문서가 **무엇에 관한 것인지**로 분류한다. 파일명·경로·
확장자는 근거가 아니라 힌트다 — `final_v3_REAL.pdf`는 제목이 아니다.

### 3.2 범주는 장서에서 자란다

미리 만든 범주 목록이 없다. 있는 것으로부터 범주가 생긴다. 장서에 계약서만 있으면 계약서를
나누는 축이 생기고, 논문만 있으면 논문을 나누는 축이 생긴다.

### 3.3 훑을 수 있어야 한다

찾는 방식이 "읽고, 좁히고, 열어본다"이므로 **두 가지가 동시에 참**이어야 한다:

| | 깨지면 | 한계의 출처 |
|---|---|---|
| **어떤 폴더도 너무 크지 않다** | 그 안에서 다시 전수 검색 — 정리한 의미가 없어짐 | 한 폴더의 목록이 한 번에 다뤄질 수 있는 크기 |
| **어떤 층도 너무 넓지 않다** | 형제 폴더 카드를 다 못 읽어서 고를 수 없음 | 형제 카드 전체가 한 번에 읽힐 수 있는 양 |

**두 제약은 서로 반대로 당긴다.** 폴더를 잘게 쪼개면 층이 넓어지고, 층을 좁히면 폴더가
커진다. 둘 다 만족시키는 방법은 **깊이**뿐이다. 그래서 트리는 넓지도 크지도 않고 깊다.

한계값은 사람의 감이 아니라 **읽는 쪽의 예산**에서 나온다. 이 값들은 §6의 검증 기준이지
코드의 판단 기준이 아니다(§6.1).

### 3.4 넓게 넣고, 자라면 쪼갠다

**이것이 Bismuth의 핵심 동작이며, 배치 정확도보다 중요하다.**

두 동작이 다 있어야 한다:

- **배치** — 새 문서를 현재 트리의 맞는 자리에 넣는다.
- **세분화** — 폴더 안에 구분이 보이면 하위로 나누고, 그 안의 문서들을 옮긴다.

세분화가 없으면 첫 배치가 영구적이 되고, 그래서 **먼저 들어온 문서가 트리를 결정한다.**
세분화가 있으면 첫 배치는 잠정값이 되고, 틀려도 나중에 고쳐진다.

#### 한 문서는 선반의 근거가 될 수 없다

우리가 만드는 것은 분류표가 이미 있는 도서관이 아니라 **이제 막 생기는 도서관**이다. 책은
한 권씩 들어오고, 분류 체계는 아직 없다(§3.2).

배치는 현재 트리 안의 위치만 고른다. 기존 표지판이 맞으면 그 폴더로 가고, 맞는 표지판이 없으면
루트 또는 현재 부모에 둔다. **배치 에이전트는 폴더를 만들지 않는다.** 한 문서만으로 만든 넓은
부류는 문서 형식처럼 장서 전체를 삼킬 수 있고, 좁은 부류는 그 문서의 제목을 다시 쓰게 된다.
둘 다 다른 문서와의 대조 없이는 구분할 수 없다.

새 선반은 파일이 놓인 뒤 별도의 구조 하네스가 같은 부모에 직접 놓인 여러 문서와 기존 형제를
함께 보고 만든다. 첫 경계는 같은 축 질문에 답하는 최소 두 형제 부류와 각 구성원을 구조화 출력으로
제안한다. 이후 새 부류는 저장된 질문의 다른 답만 제안한다. 각 부류가 최소 두 문서를 묶는지,
축과 탐색성이 유효한지를 독립 검증한 뒤에만 원본과 사이드카를 한 트랜잭션으로 옮긴다.

#### 확신이 없으면 루트다

**"어디 둘지 모르겠다"의 올바른 답은 대기가 아니라 루트다.** 루트는 아직 구분이 그어지지
않은 자리이고, 확신이 없다는 건 정확히 그 뜻이다. 그러니 배치에 **확신 임계값이 없다** —
넘고 못 넘고를 가릴 선이 없다. 확신도는 기록하되 문을 막는 데 쓰지 않는다.

**인박스에 남는 것은 읽지 못한 문서뿐이다.** 스캔본, 깨진 파일, 지원하지 않는 형식. 읽을 수
있으면 무엇이든 놓인다. 읽지 못한 것을 루트에 섞으면 세분화가 잡음에서 범주를 만들려 든다.

#### 배치는 문서 하나에 격리된 도구 에이전트다

배치 에이전트는 한 번에 새 문서 하나만 책임진다. 실제 현재 폴더 경로와 표지판, 범용 검색으로
좁힌 관련 카드만 주소 가능한 ID로 받고, 필요할 때 폴더나 관련 카드의 근거를 추가로 읽는다.
전체 장서를 재분할하거나 관계없는 폴더를 검토하지 않는다.

최종 행동은 기존 폴더 선택 또는 현재 위치 유지뿐이다. 모델은 원시 경로를 이동 명령으로 쓰지
않는다. 애플리케이션이 요청별 ID를 실제 폴더로 해석한다. 승인된 terminal tool 호출 하나만 파일
작업으로 이어진다. 에이전트가 유효한 결론 없이 끝나면 기존
표지판만 걷는 닫힌 선택 경로로 안전하게 폴백하며, 그 실패가 원본을 잃게 하지 않는다.

#### 세분화는 이름도 고치고 문서도 옮긴다

- **이름은 표본이 아니라 부류를 가리킨다.** 폴더 이름은 그 폴더가 담게 될 것들의 공통점이지
  그 안 문서 하나의 제목이 아니다.
- **쪼갤 때 부모 이름도 다시 본다.** 내용이 이름과 어긋나 있으면 이름을 고친다. 쪼개기만
  해서는 잘못 붙은 이름이 남는다.
- **세분화는 그 폴더에 이미 있던 문서를 재배치한다.** 새로 들어오는 문서에만 적용하면 먼저
  들어온 문서가 자리를 차지한 상태가 그대로 남고, §3.5가 성립하지 않는다.
- **기존 경계를 교체하는 기준은 '다른 분류도 가능함'이 아니라 '현재 실패를 실제로 고침'이다.**
  새 계획은 현재 표지판과 나란히 비교해 탐색성이 명백히 좋아지고, 이용자가 구조를 다시 익힐
  비용을 감수할 가치가 있을 때만 적용한다.
- **폴더 이름은 축의 답이어야 한다.** `규정 대상` 같은 축 이름 자체를 자식 폴더명으로 반복한
  계획은 구분을 만들지 못하므로 적용하지 않는다. 폴더 노트도 후보·잔여·검토 과정을 늘어놓지
  않고 그 폴더에 들어갈 문서를 긍정적으로 판별하는 짧은 표지판이어야 한다.
- **폴더 노트는 현재 목록의 요약이 아니라 안정적인 범위 계약이다.** 파일이 하나 추가·삭제·이동될
  때마다 다시 쓰지 않는다. 누적된 증거가 기존 범위를 반박해 경계를 새로 만들거나 교체할 때만
  구조와 함께 바꾼다. 자유서술 길이를 의미 검증 기준으로 삼지 않는다. 관리 경계는 부모의 분류
  기준·질문·이 폴더의 답을 구조화해 저장하고, 모델 생성 자체의 폭주는 전송 계층의 토큰·시간
  상한과 실패 격리로 막는다.
- **장서 전체성은 단일 컨텍스트를 뜻하지 않는다.** 큰 경계는 모든 직속 표지판과 모든 문서 카드를
  도구로 페이지를 넘겨 조사하고, 필요한 원문·폴더 노트를 선택적으로 읽는다. 의미 계획은 장서
  전체에 대한 하나의 에이전트 판단으로 유지하되, 파일 작업은 완성된 shadow plan이 제출된 뒤에만
  만든다. 코드는 경로·중복·충돌·형제 경계를 가상 적용으로 검증한 뒤 하나의 트랜잭션으로 적용한다.
- **검토 대상은 설명이 아니라 실제 적용 후보여야 한다.** 부모에 남은 문서의 기존 자식 배치,
  잘못된 기존 형제 사이의 재배치, 최초 경계 생성, 기존 경계의 형제 추가, 축 교체는 서로 다른
  권한이며 하나의 모호한 계획 타입으로 합치지 않는다. 형제 사이 재배치는 기존 표지판을 다시
  쓰거나 경계 값을 없애지 못한다. 그 정도의 변경이 필요하면 축 교체 권한으로 완전하게 제안한다.
  코드가 경로와 ID만 검증한 동일한 후보를 독립 컨텍스트의 경계·membership 검토자가 각각 반증하고, 형제 포함·중첩·추상화
  수준 불일치·catch-all·관련 문서 분리·강제 배치를 발견하면 적용 전에 거절한다. 카드 유사 신호는
  반례를 찾기 위한 증거 검색에만 쓰며 의미 판정을 대신하지 않는다.

#### 업로드 배치는 문서별 증분 배치의 운송 수단일 뿐이다

여러 파일을 한 번에 올려도 구조 판단 단위는 항상 문서 한 건이다. 원문 추출과 카드 준비는 설정된
동시성 안에서 겹칠 수 있지만, 실제 placement agent 호출과 파일 커밋은 입력 순서대로 직렬 실행한다.
따라서 문서 N+1은 문서 N의 배치 뒤 로컬 구조 하네스가 재사용하거나 만든 실제 폴더와 표지판을 본다.

30건 window, pending/deferred 큐, 업로드 종료 tail flush, 전역 planner/critic은 자동 인제스트 경로에
없다. 한 문서의 모델 실패가 다른 문서의 배치를 막거나 전체 구조 계획을 롤백하지 않는다. 이미 존재하는
동일 family의 현재 목적지는 모델 호출 전에 결정론적으로 재사용한다. 공통 위치가 잠정 루트여도
나중 문서를 다른 선반으로 먼저 갈라놓지 않고, 구조 하네스가 family 전체를 함께 이동하게 한다.

전역 organizer와 shadow-plan 검증기는 명시적인 진단용으로만 남고 새 파일 도착으로 자동 실행되지
않는다. 자동 구조 변경은 새 문서가 실제로 놓인 한 부모와 그 조상의 현재 경계 검토로 제한된다.
고정 건수 window나 전체 업로드 계획은 사용하지 않는다.

업로드 종료 시에는 증분 순서가 만든 정보 비대칭만 보수적으로 보정한다. 루트의 한 atomic family는
동일한 직속 형제 표지판 집합에서 한 번만 닫힌 선택을 받으며, 자연스러운 상위 주제가 아직 없으면
`NEW_SIBLING`으로 남는다. 다른 문서 이동 뒤 같은 family를 같은 표지판으로 다시 물어 언젠가 거짓
양성이 나올 때까지 반복하지 않는다. 마지막으로 **이번 업로드의 파일만** 완성된 최상위 형제 전체와
대조한다. 이미 배치된 문서를 다른 형제로 옮기려면 다른 표지 선택, 현재 표지보다 명백히 낫다는
독립 비교, family 구성원 전원의 목적지 적합이 모두 성립해야 한다. 하나라도 애매하면 현재 위치를
유지한다. 이 단계는 전체 장서 재구조화나 과거 문서 일괄 재분류가 아니다.

### 3.4.1 모델 출력도 신뢰 경계 안에 둔다

배치의 정상 결론은 자유 JSON이나 경로 문자열이 아니라 `finish_placement` tool schema다. 기존
폴더/부모는 요청별 핸들로만 지정하고 새 폴더는 한 경로 조각만 제출한다. 모델이 보고하는 confidence는
배치 근거도 안전장치도 아니므로 요청하거나 저장하지 않는다. terminal tool을 완료하지 못한 실행은
진단 로그를 남기고, 기존 자식 ID/`STAY`만 사용하는 닫힌 표지판 걷기로 폴백한다. 카드가 이미 만들어진
읽을 수 있는 문서를 에이전트 판단만으로 `_inbox`에 보내지 않는다.

모든 모델 생성에는 작업별 출력 토큰 상한이 있다. Agent 호출의 일반 기본값은 32,000이고, 설정으로
더 낮추지 않은 요청이 `finish_reason=length`에 닿으면 같은 요청을 64,000으로 한 번 깨끗하게
재시도한다. 64,000에서도 잘리면 직전 출력을 보존한 채 사과·요약 없이 이어 쓰도록 최대 세 번
복구한다. 이 값은 무한 생성에 대한 전송 계층 차단기이며
폴더 이름이나 분류 의미를 길이로 판정하는 규칙이 아니다. 구조화 호출은 전체 응답을 로그에
보존하되 복구 요청에 넣는 실패 출력은 제한한다. 스트림은 마지막 청크 이후의 inactivity
제한과, 출력이 계속되어도 갱신되지 않는 절대 실행시간 제한을 함께 적용한다. 명백한 반복
출력은 클라이언트가 중단한다. 짧은 suffix 반복뿐 아니라 동일한 긴 단어 열이 여러 번 되풀이되는 agent
prose도 감지한다. 탐색과 결론은 같은 transcript의 turn을 바꿔 재사용하지 않는다. 탐색이 끝나면 실제
tool observation만 평탄화하여 새 결론 요청을 만들고, 그 요청에는 결론 도구만 제공하고 tool choice를
강제한다. 읽기 전용 탐색에는 별도 `finish_exploration`을 제공해 뒤 단계의 terminal tool 이름을
추측하지 않게 한다. 따라서 과거 탐색 도구 이름이 현재 schema와 충돌하지 않는다. 거절된 결론 도구 호출은 완료로
세지 않으며, 승인된 결론 도구만 종료한다. active-context 압축과 높은 max-turn 비상 차단기는 유지하지만
낮은 turn 수에 도달했다고 같은 transcript의 도구 schema를 축소하지 않는다. 전송 오류의 일반 자동
재시도는 하지 않으며 위 `length` 복구만 별도 정책으로 수행한다.

장문 카드의 후속 window는 장서 분류에 필요한 **작은 델타**만 생성한다. summary는 전체 누적 내용을
3~4문장으로 다시 쓰되, 새 메타데이터는 토픽 4개·엔터티 8개·키워드 8개·질문 4개 이내이며 라벨은
80자 이내다. 긴 조문 목록을 한 라벨로 이어 붙이거나 같은 문구로 배열을 채우는 것은 계약 위반이다.
반복 차단기는 최후의 전송 안전장치로 유지하지만, 정상 요청 스키마 자체가 반복할 출력 공간을 주지
않아야 한다.

진단 가능한 모든 LLM 실행은 요청과 응답을 같은 실행 단위로 다시 연결할 수 있어야 한다. 로그에는
컨텍스트 압축과 어댑터 정규화까지 끝난 뒤 모델에게 실제 전달된 system/user 메시지와 도구 정의,
모델·response format·temperature·top_p·top_k 등 실제 생성 설정, 모델이 반환한 raw stream chunk·
content·reasoning content·tool call·finish reason·중단 및 오류, 도구의 실제 인자와 결과,
애플리케이션 검증 결과와 최종 파일 작업을 남긴다. 문서 ID, 유지보수 source·window·stage와 시각을
기록하여 서로 다른 호출이나 실행의 데이터를 섞지 않고 재구성할 수 있어야 한다. API key,
Authorization, Cookie 같은 인증정보는 기록하지 않는다.

로그는 다음 세 계층으로 나눈다.

- `logs/`의 `bismuth.log`, `trace.jsonl`, `llm.jsonl`은 현재 실행을 빠르게 보는 compact view다.
  `llm.jsonl` 한 줄은 호출 인덱스이며 대용량 raw chunk나 누적 prompt를 직접 품지 않는다.
- `logs/runs/<run_id>/timeline.jsonl`은 모든 단계가 공유하는 정규화된 실행 순서다. 각 이벤트는
  `schema_version`, `run_id`를 갖고, 해당하는 경우 `workflow_id`, `window_id`, `agent_run_id`,
  `stage`, `turn`, `call_id`, `tool_call_id`, `document_id`를 갖는다.
- 정확한 LLM request/response, 전체 tool result와 raw provider stream은 실행 디렉터리의
  `calls/`, `tools/`, `streams/` artifact로 분리한다. timeline과 LLM 인덱스는 상대경로, 크기와
  hash로 이를 가리킨다. raw stream은 무손실 압축할 수 있지만 내용은 자르지 않는다.

각 실행 디렉터리에는 로그 스키마 버전, 앱·Python 버전, 실행 시각, 모델과 실제 생성 설정,
artifact 경로를 담은 `manifest.json`을 둔다. 재시작은 최상위 current view만 초기화하고 이전
`runs/<run_id>`는 덮어쓰지 않는다. 운영 실행, 일회성 LLM 실험, 외부 서버 로그는 같은 실행
timeline에 섞지 않는다.

#### 문서별 에이전트의 컨텍스트와 권한은 닫혀 있다

각 배치 실행에는 현재 폴더를 가리키는 `F...` 핸들과 범용 유사도 검색으로 좁힌 관련 카드의 `D...`
핸들만 있다. 관련도 계산은 읽을 증거를 줄이는 검색일 뿐 목적지를 결정하지 않는다. 에이전트는
`inspect_folder`, `inspect_document`로 이 핸들만 읽고 `finish_placement`로 한 행동을 제출한다.
과거 planner의 `tree`, `inventory`, `related`, `submit_plan` 도구는 이 transcript에 노출하지 않는다.
목적지 표지만으로 결론을 낼 수 없을 때도 증거 조회는 폴더 또는 관련 카드 한 건으로 끝난다.
그 조회 뒤에는 탐색 단계가 닫히고 `finish_placement`만 제출한다. 이미 읽은 증거를 되읽거나
모든 후보를 순회하는 것은 유효한 placement 절차가 아니다.

`finish_placement`는 이미 존재하는 폴더 핸들만 받을 수 있다. 새 폴더명, companion 이동, 경계 수정
필드는 스키마에 존재하지 않는다. 따라서 첫 문서의 판단이 이후 구조의 선례가 되는 경로 자체가 없다.

폴더 생성은 구조 하네스만 수행한다. 아직 나뉘지 않은 부모의 첫 경계는 축과 **최소 두 형제
부류를 동시에** 제안해야 한다. 각 부류의 `Members`를 별도로 정하고 순수 도메인 검증과 반증형
semantic boundary audit가 전체 형제 후보를 승인했을 때만 적용한다. 한 부류만으로 축을 영구
고정할 수 없다. 남은 문서는 부모에 그대로 둔다. 기존 형제가 있으면 저장된 축 이름뿐 아니라
저장된 축 질문 원문을 변경 없이 전달하고, 그 질문의 다른 답만 추가할 수 있으며 전체 형제를
함께 감사한다.

동일 제목·개정판·하위 문서 family가 이미 실제 선반 또는 잠정 루트에 모여 있으면 그 현재 목적지를
우선한다. 루트 family도 다음 문서가 임의의 좁은 선반으로 먼저 갈라지지 않도록 함께 대기한다.
이 결정은 새 모델 호출보다 먼저 적용되며, 카탈로그의 과거 placement가 아니라 디스크의 colocated
sidecar로 현재 위치를 확인한다. 구조 하네스의 제안도 같은 부모에 있는 family를 서로 다른
형제로 나눌 수 없다. 일부만 한 형제에 선택된 계획을 코드가 암묵적으로 확장하지 않으며,
그 계획 전체를 거절해 모델이 family와 양립하는 경계를 다시 제안하게 한다.

### 3.5 순서에 휘둘리지 않는다

**같은 장서를 다른 순서로 넣으면 비슷한 트리가 나와야 한다.**

받는 순서에 따라 다른 도서관을 만드는 사서는 사서가 아니다. 이건 §3.4가 필요한 이유를
검증 가능한 형태로 쓴 것이며, 도메인 지식 없이 측정된다(§6.2).

### 3.6 폴더 노트는 판별식이다

폴더 노트는 장식이 아니라 **찾는 쪽이 후보를 좁히는 데 쓰는 유일한 단서**다. 그래서 "여기
무엇이 있다"만으로는 부족하고, **형제 폴더가 아니라 왜 여기인지**가 서 있어야 한다. 배제할
수 있어야 훑는 비용이 준다. 관리 폴더는 부모가 물은 분류 질문, 그 질문의 기준, 이 폴더가
대표하는 답을 보존한다. 현재 파일명·개수·도착 순서 같은 인벤토리 사실은 그 계약에 넣지 않는다.

### 3.7 결과물은 진짜 파일이다

폴더와 파일이 산출물이다. Bismuth를 지워도 정리된 문서는 남는다. 인덱스나 DB에만 존재하는
구조는 이 스펙을 만족하지 않는다 — 찾는 쪽이 `ls`/`read`/`grep`만 있어도 되어야 한다.

## 4. 불변식

깨지면 버그다.

1. **원본은 수정되지 않는다.** 이동은 하되 내용은 건드리지 않는다.
2. **모든 변경은 되돌릴 수 있다.**
3. **문서를 잃지 않는다.** 어떤 실패에서도 파일은 남는다.
4. **읽지 못한 문서는 조용히 사라지지 않는다.** 인박스에서 눈에 띄게 기다린다.
5. **구조는 디스크가 진실이다.** 캐시와 디스크가 갈리면 디스크를 따른다.

## 5. 비목표

명시적으로 하지 않는 것. 이유가 붙어 있고, 이유가 사라지면 다시 논의한다.

- **특정 도메인·조직·사업에 맞춘 분류** — §2.
- **사람이 구조를 미리 정하는 단계** — 현재 범위 밖. 사서 역할은 AI가 한다. 사람의 교정·
  승인은 고도화 영역으로 미룬다.
- **완벽한 첫 배치** — §3.4가 첫 배치를 잠정값으로 만든다. 첫 판단을 더 똑똑하게 만드는
  쪽으로 문제를 풀지 않는다.
- **여러 트리 동시 유지** — 파일시스템은 트리 하나다. 다른 축으로 찾는 것은 노트와
  사이드카가 grep 되는 것으로 해결한다.
- **검색 자체** — Bismuth는 찾기 좋은 구조를 만든다. 찾는 것은 그 구조를 쓰는 쪽의 일이다.

## 6. 수용 기준

### 6.1 임계값은 검증 기준이지 판단 기준이 아니다

**코드는 "50개가 넘으면 쪼갠다" 같은 규칙으로 판단하지 않는다.** 구조를 나눌지 말지는 모델이
현재 상태를 보고 판단한다. 개수를 세어 임계값과 비교하는 것은 도메인마다 맞는 값이 달라
휴리스틱이 되고, §2를 어긴다.

단, 적용 불변식에서 수학적으로 불가능한 계획은 모델을 호출하기 전에 건너뛸 수 있다. 예를 들어
새 서가 하나에 문서 한 건을 금지하고 최소 두 형제 범주를 요구한다면 네 건 미만 장서는 유효한
새 경계를 만들 수 없다. 이것은 "네 건이면 나눈다"는 의미 판단이 아니라 불가능한 파일 계획을
호출하지 않는 것이다.

아래 숫자는 **결과가 쓸 만한지 밖에서 재는 자**다. 코드가 이 숫자를 읽어서는 안 된다.

### 6.2 측정 항목

| 항목 | 재는 법 | 목표 |
|---|---|---|
| **순서 독립성** | 같은 장서를 서로 다른 순서로 두 번 인제스트. **문서 쌍이 같은 폴더에 묶였는지로 비교한다** — 폴더 이름이 아니라 묶임이 같은지를 본다 | 미정 — 먼저 현재 값을 잰다 |
| **폴더 크기 분포** | 잎 폴더별 문서 수의 중앙값·최대값 | 최대값이 훑기 한계 아래 |
| **층별 분기 수** | 각 폴더의 직속 하위 폴더 수의 최대값 | 형제 카드 전체가 한 번에 읽히는 양 아래 |
| **미분류율** | 인박스에 남은 문서 / 전체 | 낮을수록 좋음. 높으면 §3.4가 작동하지 않는 것 |
| **검색 정확도** | 질의 집합에 대해, 찾는 쪽이 정답 폴더를 1순위로 고르는 비율 | 미정 — 질의 집합부터 필요 |

**목표값이 "미정"인 항목은 아직 정하지 않았다는 뜻이고, 그 자체가 스펙의 일부다.** 지어낸
숫자를 채우지 않는다. 현재 값을 먼저 측정하고, 개선 폭으로 정한다.

### 6.3 디버깅과 개선은 실제 실행 증거에서 시작한다

LLM 동작, 분류 품질, 실패, 지연을 분석할 때 최종 UI 문구, 체크포인트, 토큰 합계, 결과 트리나
소스 코드만으로 원인을 단정하지 않는다. 그것들은 결과와 가설을 알려 주지만, 모델이 실제로 무엇을
보고 어떤 판단을 했는지는 증명하지 않는다.

원인 분석과 그에 따른 개선 작업 전에 최소한 다음 순서로 해당 실행을 재구성한다.

1. 분석할 실행의 시각, source, window, stage, scope와 문서 ID를 먼저 특정한다.
2. 각 단계에서 모델에게 **실제로 전달된 입력**을 확인한다. 컨텍스트 투영 이후의 system/user 메시지,
   도구 정의, 모델과 생성 설정, 읽을 수 있었던 문서 핸들과 도구 결과를 포함한다.
3. 그 입력에 대한 **실제 출력**을 확인한다. raw content와 reasoning content, tool call과 인자,
   finish reason, 잘림·반복·timeout·transport 오류를 포함한다.
4. 모델 출력 뒤의 애플리케이션 처리를 연결한다. 도구 결과, 결정론적 검증, semantic critic finding,
   후보 수정, 적용 또는 무변경 결정, 체크포인트와 실제 파일시스템 결과를 순서대로 대조한다.
5. 지연 분석은 단계별 시작·종료 시각과 호출별 elapsed time으로 계산한다. 로그 줄 간격이나 총 토큰만
   보고 어느 단계가 느렸다고 추정하지 않는다.

분석 보고서는 단계별로 `실제 입력 → 실제 출력 → 도구/검증 결과 → 최종 영향`을 추적할 수 있어야
한다. 관측된 사실, 코드에서 도출한 해석, 아직 증거가 없는 가설을 명시적으로 구분한다. raw 로그가
없거나 실행 ID를 연결할 수 없으면 그 범위의 분석은 **미확인**이라고 보고하고, 추측을 확정 원인처럼
말하거나 그 추측만으로 의미 로직을 변경하지 않는다.

로그 파일이 비어 있는지도 디렉터리 메타데이터만으로 판단하지 않는다. 실제 byte stream을 읽거나
신뢰할 수 있는 byte count로 확인한다. 특히 실행 중인 Windows 파일의 표시 크기와 수정 시각은 늦게
갱신될 수 있다.

개선안은 위에서 관측한 실패 단계와 변경할 코드 경계를 일대일로 연결해야 한다. 가능한 경우 같은
입력 또는 해당 raw trace에서 축약한 재현 fixture로 변경 전 실패와 변경 후 결과를 비교한다. Fake LLM
테스트와 정적 검사는 계약과 안전성을 검증할 수 있지만 실제 모델의 분류 품질이 개선됐다는 근거로
대체하지 않는다.

반복 출력은 기본적으로 sampling 값이나 사후 문자열 제거로 해결하지 않는다. 먼저 반복이 시작된
시점의 실행 상태를 재구성해, 이미 끝난 단계에서 자유서술이 허용되었는지, 종료 도구가 실제 terminal로
등록되었는지, JSON 배열만 제한되고 항목 문자열은 무제한이었는지, 이전 단계의 도구와 지시가 다음
단계에 누출되었는지를 확인한다. 단계의 권한·종료·스키마 계약을 좁혀 같은 생성 상태가 생기지 않게
하는 것이 우선이며, 반복 감지와 출력 한도는 계약 위반을 기록하고 비용을 제한하는 회로 차단기다.

모델 컨텍스트는 `입력 + 요청 출력`의 합으로 예약한다. 32K·64K 같은 출력값은 상한일 뿐이며, 매
호출의 실제 system/messages/tool schema 입력 추정치와 안전 여유를 context window에서 뺀 나머지만
출력 슬롯으로 요청한다. 계산값은 request artifact에 남겨 context overflow를 원문으로 검증할 수
있어야 한다.

### 6.4 이 스펙이 통과했다고 말할 수 있는 순간

장서를 통째로 넣었을 때, 그 결과 트리 위에서 찾는 쪽이 **정리 전보다 확실히 잘 찾고**,
**순서를 바꿔 다시 넣어도 그 성질이 유지되는** 때다. 나머지 항목은 그 두 가지를 무너뜨리지
않았는지 보는 보조 지표다.

## 7. 아직 정하지 않은 것

여기 있는 항목은 "모른다"가 아니라 **"의도적으로 미룸"**이다.

세분화의 설계는 [`docs/spec/subdivision.md`](docs/spec/subdivision.md)에 있다. 거기서도
안 정한 것은 그 문서 끝에 적혀 있다.

- **인박스가 어디까지 남나** — 루트가 정상 답이 되면(§3.4) "확신이 낮아 파킹"은 대부분
  사라진다. 읽을 수 없는 문서만 남을 텐데 그 선을 아직 안 그었다.
- **§6.2의 목표값들** — 측정 후.
- **사람의 교정 지점** — §5에서 미룬 것.

## 8. 용어

| 말 | 뜻 |
|---|---|
| **장서** | 정리 대상 문서 전체 |
| **카드** | 문서 하나에 대해 모델이 파악한 것 (무엇인지, 무엇에 관한 것인지) |
| **폴더 노트** | 폴더 하나가 무엇을 담는지에 대한 서술. 찾는 쪽의 판별식(§3.6) |
| **사이드카** | 원본 옆에 두는 grep 가능한 텍스트 |
| **배치** | 새 문서를 트리의 자리에 넣는 것 |
| **세분화** | 커진 폴더를 하위로 나누는 것 |
| **찾는 쪽** | 이 구조 위에서 검색하는 사람 또는 에이전트 |
